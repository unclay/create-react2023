import React from "react";
import { createRoot } from "react-dom/client";

const root = createRoot(document.querySelector("#app"));
root.render(
  <div>
    <h5>React App: </h5>
    <div>code from code/jsx</div>
  </div>
);
